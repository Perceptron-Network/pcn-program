# Local validator is the safe default. Never point this app at mainnet without an explicit review.
VITE_RPC_URL=http://127.0.0.1:8899
VITE_CLUSTER=localnet

# Optional visual preview data. Live RPC data remains the default.
VITE_DEMO_MODE=false
